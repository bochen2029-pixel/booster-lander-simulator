# ROADMAP — Booster Lander (perpetual, compaction-proof plan)

> **You are reading this because the SessionStart hook forced it in front of you. Good. Follow
> the RESUME PROTOCOL, then work the first unchecked box. This file + the handoff live-log +
> MEMORY.md are the source of truth; trust files over recollection; verify against disk.**

## RESUME PROTOCOL (do this FIRST, every fresh/compacted session)
1. Read `C:\Users\user\.claude\projects\C--Booster-Lander-Simulator\memory\MEMORY.md` — the top ADR line is your state.
2. Read `HANDOFF_2026-07-19_MORNING_AUTORUN.md` §0 **LIVE LOG** (top, newest-first) — the minute-by-minute.
3. Verify disk: `git log --oneline -3` (expect the HEAD the memory names) · `.\build\bin\Release\booster-core.exe --selftest` → PASS.
4. Find the **first ⬜ box** below = your next step. Proceed under **standing autonomy** (Bo Chen: momentum, honest nulls, ledger discipline, byte-gates). Stop only on a hard blocker → write it into this file + ping the operator.
5. **Update this file (check boxes, add notes) AND the handoff live-log at every state change.** Keep the perpetual map current — that is the whole point.

## STATUS (update on every ADR)
- **2026-09-11 03:30: ①b SEED 7 REPLICATES SEED 42 EXACTLY — 52/60, 8 crashed, FUEL 2, on both.** (s42 40P·10G·2H · s7 37P·14G·1H — different quality split, *identical* landed and crash counts.) **The 8-draw cost of blindness is structural, not a seed artifact.** Held-out mean lateral 0.39 / 0.72 m. Seed 99 still flying.
- **2026-09-11 03:30: D-050 INTERIM — CONDITIONING IS BUYING ALMOST NOTHING SO FAR, and the honest check is PAIRED.** At iter 2/14, best w=149/360 on seeds [5004,5005]. Re-scoring the D-047 constant (W=0) on **those same seeds**: **87/120 landed, w=143/360** — against D-050's **87/120 landed, w=149/360**. **Identical landed count; +6 weighted.** It is converting a few HARD arrivals into GOOD and nothing more. That is the pre-registered *"no better than the constant"* branch forming, and it is reported at iteration 2 rather than saved for the end. **AND A DESIGN HAZARD THIS EXPOSED:** the seed pair rotates each generation (the anti-overfit fix), so scores across generations are **not comparable** — the constant alone scores **85 / 68 / 87** on the three pairs. `gbest` as a running maximum therefore mostly selects the easiest seed pair, not the best policy. The final winner **must** be re-scored on a fixed pool before any held-out flight; the script already prints that warning, but `best.json` is a lucky-seed artifact until it is.
- **2026-09-11: ①b IS IN — THE 180/180 WAS NOT MADE OF CLAIRVOYANCE. Blind GM_RFLY = 52/60 (s42).** On the identical sixty faults: clairvoyant **60/60**, blind **52/60** (40 PERFECT · 10 GOOD · lat **0.39 m** vs 0.32 m). **Hiding the future fault costs eight draws, not fifty — and it costs whole flights, not accuracy.** New failure modes are honest: FUEL 2, LOC 3, neither of which the clairvoyant arm produced. **Exceeds the high pre-registered branch (~45/60); the ~6/60 branch is dead.** THREE CONSEQUENCES: **(1) a deployable flight computer exists on this disk today** — the blind search carries no privilege, and R2b already measured the budget dropping 8× with no rate loss (**~0.4 s/replan** against a **0.1 Hz** outer loop), so it is real-time by ~25× and legal; the "76 s vs 10 µs" framing that motivated three arcs of distillation was measuring against a 500 Hz *inner*-loop bar a mission-layer setpoint never had. **(2) Phase 3's 0/12 has one surviving explanation** — the teacher is strong AND mostly legal, so privilege was never the barrier; what remains is D-047's knife-edge (eight identical flights flip on a 3.5% gain change), which makes regression onto its conditional mean ill-posed *by construction*. **(3) Adaptation earns its keep** — 52/60 blind vs the optimized constant's ~34/60 is **+18 draws of real state-dependence**, so D-050 is aimed at something that exists. Seeds 7/99 still flying. Full entry: D-046 addendum 3.
- **2026-09-09: D-046 IS IN — GM_RFLY sweeps the engine-out frontier 180/180.** All three held-out seeds 60/60 (147 PERFECT · 33 GOOD · **0 HARD · 0 TIPPED · 0 CRASHED · 0 faults · 0 crash causes**), mean lateral miss **0.32/0.34/0.33 m** — agreeing to 2 cm across independent seeds. Same-binary control the same day: **MPPI 4/60, with 50 of its 56 crashes off-pad.** (E0's "1/60" was pre-D-030 and was a cross-version quote; corrected in-ledger.) **D-027's `in-frontier ≈ 1.000` is not theoretical — something on this disk reaches it.** But GM_RFLY is a **privileged oracle**: `rfly_eval_candidate` copies the Sim including `eo_engine`/`eo_time`, so the search flies the true realization and knows which engine fails and when, at 76 s/flight. **180/180 is an upper bound, not a flight computer.** The open problem was never capability — it is **compression and latency under non-privileged observation**, which reframes Phase 3's 0/12: distillation did not fail against a weak teacher, it failed against a teacher at the ceiling. **A prediction of mine is on the record as falsified:** I argued organ ③ was where the number was; under the search the entry-burn-cut cluster is *gone* (zero off-pad), so that failure belongs to the reactive/MPPI controller, not to physics.
- **2026-09-08: THE FRONTIER ARC OPENED (D-046).** Six-week gap since the last repo write. The architectural finding: `grep "frontier\|D_phys\|BRS" core/*.c core/*.h` returns **comments only** — §9.9's yardstick was declared THE metric in D-019, has a working oracle (`runs/sandbox/ceiling{,_eo}.c`), produced the estate's most important number in D-027 (**in-frontier ≈ 1.000 ⇒ ~59/60 claimable vs ~9/60 achieved**) — and was **never wired into the flight code**. Every controller built here answers *"what do I do now?"*; none computes *"what can I still reach?"*. **D-046 batch FLYING** (`runs/d046_rfly_frontier.ps1`, ~3.8 h, marker `D046-FRONTIER-DONE`): GM_RFLY on the D-027 `--engine-out random` ×60 held-out battery — the run that was never done (zero grep hits), and the one that decides the next build. **HOLDS THE EXE — no C builds until the marker.**
- **2026-07-27: M4 GREEN** (D-044) — the θ̂ gain-schedule (`--rfly-theta-net`, TP_VERSION 2) clears held-out AERO **171/180 (95.0%)**, all 3 seeds ≥54. First learned controller to clear M4 (open since D-018). Search (GM_RFLY) owns the compound 36/36; learned tuning owns the envelope.
- **HEAD: D-040 + N3 FIRST LIGHT (2026-07-24; GM_RFLY sweeps the compound 36/36 held-out, three live GOOD landings on the cockpit stream, v0.3.0 released).** Canon = `CLAUDE_v2.md`. **NP_VERSION 6** (sha b4734b48).
- **THE LIVE EDGE (2026-07-25): the compound is solved by a SEARCH, not a POLICY.** GM_RFLY spends ~60 s of CEM per flight (measured, `runs/rfly_rate_s42.txt`); canon §1/§9.8/§14-N3 wants a **10 µs feedforward policy** flying the same case off the legal socket. NP_VERSION 6 is 30-in, **lateral-only**, 37k params, **0/30 on the compound** — worse than the hoverslam it steers. M4 (AERO ≥54/60 *via GM_NEURAL*), N4 perception/worlds, and a real-time cockpit are ALL gated behind that search→policy conversion. **⇒ the ORACLE-DISTILL program (§ below) is the next arc.**
- *(D-039 was the prior STATUS line: Mode 2 live target-drag — the operator drags the pad, guidance chases it.)*
- Learned policy: AERO clean **46** (teacher+2), gust **45** (teacher+7), **ENTRY clean 57/56/58** (teacher parity). No forgetting ×7.
- **D-027 + E0/E0b:** engine-out collapse is a **CONTROLLER SHORTFALL** (gilded on all 4 axes) — whole random-EO draw in-frontier ⇒ ~59/60 claimable. **v6 EO baseline = 1/0/0 (= MPPI); E1 is the real work.**
- **E1 NULL (D-029) → E1.5/D-030 DONE: the 2-engine entry-divert re-authorization LIFTS EO recovery** (reactive 0/1→**9/10**, neural 1/0/0→**8/4/2** of 60; byte-clean). Pivotal: reactive now beats neural on EO. → **E2 (MPPI teacher, D-031) AND E2' (reactive teacher, D-032) BOTH NULL — distillation is EXHAUSTED for the EO axis (both regress v6's 14/180 to 11/180; the shared policy can't absorb the EO a_lat). Neural EO plateaus at v6+D-030's 8–10/60; the ~59/60 frontier is RL-class. NP_VERSION 7 rejected both times, reverted to v6. NEXT: RL (reserved, multi-session) OR pivot to a parallel-safe showcase axis (Target Stage-1 / interactive ws channel).**
- Model: **Opus 4.8** (Fable weekly maxed — stay on Opus). Machine sleeps; detached jobs resume on wake.

## THE MAP — the engine-out arc → N3 showcase (the operator's ORIGINAL wow)
- ✅ **E0 — v6 EO baseline** (`runs\eo_baseline_v6.txt`): v6 on `--engine-out random` ×60 = **1/60 · 0/60 · 0/60** (s42/s7/s99) = dead parity with MPPI 1/60. ENTRY competence does NOT transfer to EO for free; ~59/60 claimable headroom. E1/E2 confirmed necessary.
- ✅ **E0b — oracle gilding** (`runs\eo_gild_1at11.txt`, report §8): `1@11 --mppi` = CRASHED lat 118.85 m **tilt 0.02°** fault=none → attitude held, NO tumble; pure lateral-CLOSURE failure. D-027 verdict now gilded on all 4 axes.
- ✅ **E1 — composite operator (student-warm-started MPPI): BUILT, byte-clean, NULL (D-029).** ENTRY `--engine-out random` ×60 s42: composite **1/60 == student 1/60 == teacher 1/60** — NOT an improvement operator (design §3: must not teach). Phase-attribution (`runs\e1_phase_attribution.txt`): the gross cluster (~75%) is lost at the ENTRY-BURN CUT (2-engine divert closes only ~830 m); MPPI/neural only steer POST-cut ⇒ the composite is structurally blind to it. `--mppi-warm-neural` kept default-off (D-018 pattern). ⇒ redirect to E1.5.
- ✅ **E1.5 — 2-ENGINE ENTRY-DIVERT re-authorization (D-030): DONE.** `entry_divert_step` under n_eng<3: bank cap **15°→35°** + **KR×4/KV×2.5** (byte-clean, frozen #defines; leak GREEN). EO recovery lifts mode-independently (ENTRY EO ×60): reactive **9/10** of 60 (was 0/1), neural **8/4/2** of 60 (was 1/0/0), held-out generalizes. Effective KR8/KV8.75 near-critical (baseline 2/3.5 overdamped). **Pivotal: REACTIVE now beats NEURAL (2–8) — the clean-trained policy mishandles the hot 2-engine handoff ⇒ E2.** Partial fix (~8–10×); hardest draws + terminal quality remain.
- ❌ **E2 — ENTRY engine-out DAgger round (D-031): NULL, reverted.** MPPI+D-030 teacher (~10%) distilled → NP_VERSION 7 EO **6/0/5** of 60 = 11/180, WORSE than v6+D-030's 14/180 (+ gust-A 45→44). A teacher no better than the student (v6 8/60) can't lift it (D-025 confirmed directly). Rejected → reverted to v6 (selftest KAT v6 PASS, EO 8/60 restored). **The distillation-era EO ceiling is mapped: all controllers ~8–10/60.** Datasets `s0eo2_*` + ckpt `s0eo2.pt` preserved.
- ❌ **E2' — reactive-teacher DAgger (D-032): ALSO NULL.** Built `--shadow-reactive` (log hoverslam's a_lat; byte-clean, kept default-off), retrained → NP_VERSION 7 EO **2/5/4 = 11/180**, again WORSE than v6's 14/180 (s42 8→2). BOTH teachers (MPPI + reactive) regress ⇒ **distillation is EXHAUSTED for the EO axis** (the shared 37k-param policy can't absorb the EO a_lat without a clean-vs-EO compromise). Reverted to v6. Neural EO plateaus at v6+D-030's 8–10/60.
- ✅ **E-EO-RL / N2-S2 — CONCLUDED (D-040 → D-044): the pivot shipped (GM_RFLY 36/36, verified from a clean clone), distillation closed as a measured null, M4 GREEN via θ̂.** *(full arc history:* REFRAMED 2026-07-20 as SOLVER-THEN-DISTILL; 2026-07-21 cfly sweeps the sandbox compound 16/16 GOOD+; 2026-07-23 PHASE A VERDICT: the DIRECT PORT of the sandbox law is an honest NULL (clean entry fuel-starves too; the t=0 CEM never finds a landing; the plants differ ~40% in descent time — the 16/16 certified the SEARCH, not the law-on-this-plant). NEXT = the D-040 PIVOT (drafted, `C:\Booster_Lander_CFLY\runs\D040_draft.md`): optimizer-in-the-loop over the NATIVE reactive stack's gains (hoverslam is fuel-feasible with 2-3.3 t margin; its compound miss is lateral-35-m-soft — exactly what per-scenario gain search closes; the cfly CEM machinery is proven byte-clean and reusable, only the θ→law binding changes).** The "RL-class" verdict was about weak teachers; the operator's reframe: solve the deterministic plant OFFLINE with an expensive search, distill. Two isolated lanes: **LODESTAR** (`C:\bl_opus_oracle`, `opus/robust-oracle`) and **KESTREL** (`C:\Booster_Lander_Oracle`, `oracle-solver`). **The 2026-07-21 chain (all in the LODESTAR branch):** (1) KESTREL distill-B executed = residual-distillation NULL, airtight (compound regression AND clean collapse, two nets; `c1c60da`) — the THIRD independent proof obs→action distillation from sparse per-scenario CEM fails. (2) The θ-PREDICTOR (net predicts the 9-D guidance tuning) built + scaled to 66 scenarios = **conclusive NO-GO** (all variants ≤ nominal-θ; flat learning curve ⇒ fleet-scale regression unjustified). (3) **The knife-edge QUANTIFIED** (tsens): the law's θ-basin is **±5%** on 4 of 9 coordinates — no open-loop selection can fly it; the crash mechanism = "hover-starve" (min-throttle TWR>1), fixed by the **coast-relight law** (`ff90650`). (4) **PLANT BUG found+fixed:** the sandbox touchdown check missed DECK-RISES-INTO-VEHICLE contact (heave) — level-check fix (main tree already robust). (5) **⇒ cfly = warm-started optimizer-IN-THE-LOOP** (oracle-strength solve at t=0 = the plan; small warm CEM replans every 10 s = the onboard replan; bit-exact refactor): **held-out compound scen 16..31 = 16/16 landed, 16/16 survivable, 16/16 GOOD+** (12 PERFECT, td_v 0.58–3.46, ≤2.8 m on the moving deck, tilt ≤0.8°) vs nominal-θ 4/16 and θ̂-net 2/16 on identical draws (`2e16674`). **The AlphaZero structure the operator framed: search supplies the ±5% precision no regression can; the NN's role shifts to warm-start/compute reduction.** NEXT: port cfly into the MAIN tree (Phase-3 sketch in the branch: guidance_theta + cem_local on the real Sim + ceremony) → the N3 showcase; the H200/GPU CEM (KESTREL's parked parity-gated port) makes it real-time. The `--shadow-reactive`/`--mppi-warm-neural` flags + s0eo2/s0eo3 datasets remain preserved.
- ⬜ **E3 — pairwise**: gust + engine-out together. Mixed-rung ⇒ operators in SERIES, verdict arbitrates (cbc89fe routing).
- 🟨 **N3 — THE COMPOUND SHOWCASE: FIRST LIGHT FLOWN (2026-07-24, D-040 addendum).** Three consecutive LIVE GOOD verdicts on the photoreal cockpit stream — engine-out × gust 15 × drifting target × heaving deck — including ×2 the ORIGINAL WOW: the **ENGINE OUT button pressed mid-entry-burn, journaled, answered by the async warm replans, landed on the bullseye** (GM_RFLY; pre-stream mission solve + async replans + terminal freeze; filmed by the self-verify loop via the director camera — `runs/shots/n3d_*.jpg`). **Remaining N3 garnish:** the §G.2 honest MANUFACTURED out-of-frontier failure (6–8 km / center-engine-out / two-out, per D-027) · constellation scoring vs the shrunken BRS · replay-from-journal (§10.8) · the **M4 attempt** (AERO ≥54/60 ⇒ M4 GREEN via GM_NEURAL; else the 0.70·D_phys plateau routes M4 to the plant-authority ADR).

## THE NEXT ARC — ORACLE-DISTILL: close the AlphaZero loop (search → policy → search)
*Opened 2026-07-25. Design: `runs/oracle_distill_design.md` (written 07-20, before RFLY existed — its "Phase A optimal oracle" is now BUILT and PROVEN: GM_RFLY 36/36). Two nets with DIFFERENT jobs:*
- **π (the student)** — a full-action, self-sensed policy distilled from RFLY's solved trajectories. Becomes the flight computer. Targets N3-as-policy + M4 + unlocks N4.
- **θ̂ (the prior)** — predicts RFLY's 10-D θ from the observation to **warm-start the CEM** (AlphaZero's policy-prior role, NOT a controller — LODESTAR §13 already nulled θ̂-as-controller on the sandbox's ±5% knife-edge law; as a *prior* its bar is only "beat identity warm-start"). Buys real-time replans + cheaper data-gen without renting an H200.

- ✅ **Phase 0 — hygiene (2026-07-25):** the TERMINAL FREEZE (`guidance_rfly.c` `rfly_async_poll`) was UNCOMMITTED — the load-bearing fix behind N3 first light lived only in the working tree + linked binary. Committed under the full battery: selftest PASS · TERMINAL ×200 **byte-identical** · MPPI run-1 AERO s42 **2.63/10.48** exact · RFLY compound run-0 reproduces **PERFECT td_v 1.72 / lat 0.38**. + `s0_farm.ps1 -Target` (compound-regime farms).
- ✅ **Phase 1 — the App-G v2 re-architecture event DONE (D-041, `232463b`).** Socket **30 → 39, APPEND-ONLY** (achieved specific force + body angular accel + last executed command — the honest sensor version of `eng_health`); **Tier-A full action** declared by the frozen header (`NP_ACTION_TIER`, absent ⇒ 1 ⇒ compiles out ⇒ v6 bit-identical); the **GM_RFLY oracle tap** (written after the D-009 wind-trim — the last thing that edits `a_lat`), rows carrying the CEM's **θ as privileged teacher context** for the θ̂ prior. Trainer mirrors the 55-col row + refuses v1-width data by name + `--verdict-csv`. **The signal is measured:** the engine-out at t=4.02 shows as `sf_z` **52.37→36.67 m/s²** and `wdot_y` **+0.021→−0.135 rad/s²** in the failure tick — no flag needed. Gates green throughout (selftest · TERMINAL byte · MPPI 2.63/10.48 · neural AERO 46/60 · RFLY run-0 PERFECT 0.38 **with the tap armed**). *Frame-stack DEFERRED per App-G's own rule ("add only if N3's frontier metric shows temporal inference is the binding shortfall") — one skew-risky component with unproven payoff, kept out of a one-shot event.*
- 🟨 **Phase 2 — the teacher farm FLYING** (`data/s0rf`, seeds 5000-5011 ×16, deadline-paced to 07:30, `runs/rfly_farm.ps1`). **Measured cost: ~150 s/solve** (not the 60 s the D-040 note implied — SEA-armed runs stretch toward the 200 s cap). **Honest null on the obvious lever:** 4 concurrent processes at OMP=4 = **159.6 s/run effective** vs 150 s/run single-process-full-threads ⇒ the CEM already saturates the box; the farm is SEQUENTIAL by design. Overnight ≈ 190 solves ≈ 1.2M verdict-filtered rows. If volume binds, the next lever is **θ-transfer augmentation** (re-fly a solved θ on neighbouring draws at ~1 s each, verdict-filter) — not a fleet rental.
- ✅ **PLANT BUG FIXED (D-043, 2026-07-27): the settle test is now deck-relative.** *(was: deferred mid-farm)* `sim.c`'s settling block scores ABSOLUTE kinetic energy, but a vehicle landed on a heaving deck rides the swell ⇒ `ke` never drops under the threshold ⇒ the run only ends on the `t>200` fallback. **No verdict was ever wrong** (impact_* latch at first contact, D-034; every D-040 number stands) — but ~35-40% of farm CPU simulates a parked vehicle, replanning a full warm CEM every 10 s of it. Fix = deck-relative KE (`deck_vz_live` + deck horizontal velocity, both already on the Sim), **byte-safe by construction** (SEA off ⇒ deck velocity 0 ⇒ deck-relative KE *is* absolute KE ⇒ every golden reproduces). Not taken mid-farm: byte-exact determinism outranks a 2 h saving. **Own ADR + own gate run; ~35-40% off every future farm and DAgger round.** Data half already fixed (`drop_parked_tail`, 37.2% dead rows removed, flare intact).
- ✅ **Phase 2b — the CLEAN-REGIME farm: DONE (13 jobs, 196 runs, 192 trainable).** The pre-existing datasets (`data/s0`, `s0r1`, `s0g_*`, `s0e_*`) are **v1-width (36 col) and cannot be mixed** with v2 data — `rowformat.read_rows` refuses them by name, correctly. So the standing no-regression floors (AERO≥46 / gust≥45 / ENTRY-clean≥57) are **unreachable by construction** for a compound-only corpus: the pilot scored **AERO 0/60** simply because its corpus contains no AERO_OFFSET states. ⇒ farm clean AERO / gust / clean ENTRY with `--rfly` in the v2 format and merge. The auto-gamut handles a mixed corpus (it takes the widest range present), so one merged corpus with one teacher is correct.
- ❌ **Phase 3 — CLOSED AS A MEASURED NULL (D-041 addenda 10-11, 2026-07-26).** BC + oracle-DAgger, 5 rounds + a pre-declared bounded probe, hard-stopped on its own terms: **0/12 at every round (NP7-NP12)** while DAgger itself demonstrably worked (visited-state cost −71%, rounds 1-3). Attribution, each mechanism measured: covariate shift (FIXED by DAgger) · the AUTHORITY GAP (structural — 4/10 θ dims outside the student's reach) · LABEL CONFLICT (doomed-state vs nominal answers; down-weighting failed both directions). Tree restored to v6, gates re-verified. **NEXT RUNG: R2 — θ̂-as-gain-schedule (sidesteps all three mechanisms; A/B pre-scoped: identity vs constant-mean-θ vs θ̂ vs single-t=0-solve). Awaiting operator blessing.** The showcase remains GM_RFLY (36/36 + the filmed N3 live demo).
- *(superseded)* 🟨 **Phase 3 — distill + DAgger-with-oracle. THE MECHANISM IS BUILT AND VERIFIED (`--shadow-rfly`, `3a9fbbb`):** a GM_NEURAL flight that re-solves θ **at the states the student visits** and logs the oracle's command there (plant flies the student; only the label is the oracle's). Fixed a latent bug for it — `rfly_eval_candidate` never set `c2.guidance_mode`, so a search under GM_NEURAL would have flown its candidates with the STUDENT. Verified functionally: θ on 2551/2551 rows, 5 re-solves per flight, and **42.3% of labels above the student's own ±3.2 clamp** (so demonstrably not its own command); `gbest` rises 37.8→495.5 across the flight = covariate shift measured directly. Byte-clean (default off ⇒ the D-023 MPPI shadow path; `--neural` still exactly 46/60). **NOTE (D-041 addendum 6): DAgger is MANDATORY, not polish.** The 47-run pilot reached imitation **R² 0.9966** on held-out RUNS and still flew **0/12** — td_v ~200 m/s, upright, kilometres downrange, run 0 hitting the ground with 7.1 t of fuel unburned. That is textbook covariate shift, and LODESTAR reached it independently (§10: pure BC 2/8 → +DAgger 8/8 monotonic, "the load-bearing result"). **Do not read a weak post-farm BC number as a failure of the architecture.** BC, then roll π and **re-solve θ with a warm CEM at the states π visits** (~3–8 s each) and relabel. This is the direct answer to why D-031/D-032 nulled: the relabeler is now a 36/36 expert, not a 10% one.
- ❌ **Phase 4 — SUPERSEDED (D-041 add.10-11 + D-044): the gates ran and the `--neural` compound showcase is a measured NULL (0/12 across 5 DAgger rounds); M4 GREEN arrived instead via the θ̂ gain-schedule (D-044), and the compound showcase stays GM_RFLY.** *(original text:* gates + the showcase flies on `--neural`: full battery + no-regression floors (AERO≥46 / gust≥45 / ENTRY≥57) + compound rate vs the shrunken BRS + **the M4 attempt (AERO ≥54/60 ⇒ M4 GREEN)**. Plus the operator's litmus: **hide the fault flags at eval** — if it still lands, it is inferring, not reading a cheat sheet.
- 🟨 **R2 — theta-hat gain-schedule: PARTIAL WIN (D-042, TP_VERSION 1, `67076e9`).** A 10 us learned net (`--rfly-theta-net`) drives GM_RFLY's gains each tick in place of the CEM. **The first learned artifact of this arc to beat the analytic baseline on a gated axis:** held-out AERO 131->162/180 (72.8%->90.0%), beating even a constant mean-theta on 2/3 held-out seeds (state-dependence real). Deterministic, byte-clean, TP KAT in the selftest. **M4-BORDERLINE** (90.0% aggregate; s99 56/60 clears, s42/s7 at 53). BUT the engine-out **compound stays 2/12** — the lookahead wall, triply confirmed (constant-theta probe + theta-hat + LODESTAR 13): the recovery theta is not a function of observable state. **The compound is search-necessary; GM_RFLY remains its controller (36/36 + N3 demo).**
- ✅ **M4 GREEN (D-044, TP_VERSION 2): the θ̂ gain-schedule clears held-out AERO ≥90%.** s42 **57/60**, s7 **58/60**, s99 **56/60** = **171/180 (95.0%)**, all three seeds ≥54 (vs hoverslam 131/180, TP_VERSION 1's 162/180). Open since D-018 — a LEARNED controller (`--rfly-theta-net`, a 10 µs net over the reactive stack) now realizes the authority the D-018-era samplers couldn't. More AERO oracle data was the lever (160→443 runs; regularization was null, best-val stuck at epoch 1). **Methodology note:** val_nrmse is a POOR proxy for landing rate (TP_VERSION 2 has *worse* nrmse than v1 yet lands +9). Gates all green, byte-clean (θ̂ default-off). Arrival quality: GOOD-dominant + a HARD fraction (§9.9). The N3 routing is resolved — M4's learned-policy branch is TAKEN, not the plant-authority ADR.
- ✅ **D-043 — deck-relative settle test (plant fix, byte-safe).** SEA runs terminated at the t=200 cap because the settle test scored absolute KE (D-041 add.3); now deck-RELATIVE => SEA landers settle ~124-145s. Byte-safe (SEA off identical). Saves ~35% CPU on every future SEA/compound farm + DAgger round.
- ❌ **R2b — theta-hat CEM warm-start: NULL (D-042 add.1, `--rfly-warm-net`/`--rfly-budget`, byte-clean).** The sweep found the real-time win needs NO net: **cold GM_RFLY holds 12/12 survivable from full budget to 1/8 budget** (1642->62 s, ~0.4 s/replan = real-time) because the reactive-stack theta-basin is wide. Precision degrades (10 PERFECT -> 1 PERFECT/9 GOOD/2 HARD), so it's a latency<->precision trade. **theta-hat warm-starting is negative** (12->10->9 as budget shrinks): re-seeding discards the search's accumulated theta. ⇒ **real-time GM_RFLY = reduced budget (trade precision) OR KESTREL's GPU CEM (full quality).** The NN's value is the R2 gain-schedule, not search acceleration.
- ⬜ **Phase 5 — RL fine-tune** (the reserved N2 lane), warm-started from π, §9.9 frontier as potential-based reward. ONLY if imitation plateaus short of the frontier.
- **AUDIT 2026-07-26 (D-041 addendum 9 — read it before continuing this arc):** (1) the DAgger loop was verdict-filtering away its own shadow rows (student outcome ≠ label quality) — fixed via `--dagger-data`, rounds now NP8+; (2) **the AUTHORITY GAP**: the student cannot express EKR/EKV/EBANK (entry supervisor, `sim.c:308` — the policy is never invoked during the entry burn) nor IGNM (analytic ignition trigger, `guidance_hoverslam.c:226`) — the four θ dims that decide the compound. The running DAgger loop measures the POST-CUT ceiling only. **Remediation ladder: R1 corrected-DAgger (running) → R2 θ̂-as-gain-schedule under GM_NEURAL (recommended next ADR; retrain θ̂ on the full corpus first) → R3 Tier-B ignition logit → R4 entry authority.** (3) KNOWN R1 LIMIT: DAgger collection is COMPOUND-ONLY, so even a working loop does not lift the AERO/gust/ENTRY floors — those need regime-balanced rounds or R2. (4) `--nav-noisy` caveat on the self-sensed channel documented in `policy_obs.h`. (5) GM_RFLY remains the shippable showcase controller regardless of the student's fate.
- **Pre-registered risks + kill criterion:** (1) a full-action head may regress clean flight — mixed-distribution training, v6 stays shippable, revert on floor failure (D-031/032 precedent). (2) RFLY's θ is tuned by a search that FLIES the actual realization = a privileged teacher (legal per the asymmetric teacher-student frame) — π must never see θ, and every trajectory stays verdict-filtered through the byte-exact sim. (3) **KILL:** if π ≤ hoverslam's 2/12 on held-out compound after 3 DAgger rounds, ledger the null with data and ship θ̂-as-prior + RFLY-in-the-loop as the showcase controller.

## THE FRONTIER ARC — put the reachable set in the loop (opened 2026-09-08, D-046)
*The thesis: three arcs taught nets to fly a rocket that does not know what it can still reach. The estate's base rates say why that failed — work opened by a **training run** (Phase 3 π, R2 θ̂, R2b warm-start) = 2 nulls + 1 partial; work opened by a **measurement or a deterministic fix** (D-027, D-030, D-035, D-043) ≈ 100%. So the learning goes LAST this time; the first three steps contain none.*
- ✅ **① THE FRONTIER BATCH — DONE (D-046, 2026-09-09): 180/180.** Answered ABOVE the pre-registered high branch (100%, not ~45/60) — a third outcome neither branch named. On this axis there is **no gap between the search and the frontier**. Control run same binary/day: MPPI 4/60. Full table + the falsified organ-③ prediction: D-046.
- ⬜ **①b THE BLIND-TEACHER ARM — pre-registered, not yet run. THE decisive number of this arc.** One line in `rfly_eval_candidate` (`if(!c2.eo_fired) c2.eo_engine = -1;`) hides *unfired* faults from candidate rollouts while the real flight still takes them; already-fired faults keep propagating (n_eng decremented, `sf_z` is honest self-sensed state). Default-off, byte-clean, same battery, same ×60 batch. **Declared reads: ~45/60 ⇒ clairvoyance is worth ~15 draws, the teacher has large transferable content, Phase 3 is worth re-attacking · ~6/60 ⇒ the 180/180 is MADE of clairvoyance and Phase 3's null was structural.**
  - **A THIRD READ, ADDED BEFORE THE DATA (01:30) — and it only exists because D-047 ran.** The pre-registration above was written when the only reference points were identity (9/60) and the privileged search (60/60). D-047 has since put a **zero-privilege, zero-inference constant at ~34/60 honest** (re-scored on fresh seeds, not the 42/60 in-sample figure). That creates a new and much sharper boundary: **if the blind search lands BELOW ~34/60, then an optimized constant BEATS a search that adapts but cannot foresee** — i.e. re-solving θ every 10 s, without foreknowledge, is worth *less* than simply having the right fixed gains. That would be a genuinely surprising and publishable inversion, it would retire the "adaptation is what the search buys" hypothesis outright, and it would make the constant (or its phase-scheduled successor) the deployable controller rather than any distillation target. **Above ~34/60 the search's adaptation is earning its keep; below it, the privilege was.** Neither number was available to compare against before tonight. Everything downstream (② and ④) is uninterpretable until this lands.
  - **WHY ①b MATTERS MORE THAN IT LOOKS — the 10 µs requirement may be self-imposed.** The arc has framed the gap as *"76 s/flight vs 10 µs"*, i.e. a latency problem needing distillation into a feedforward net. But **R2b already measured that away**: cold GM_RFLY holds **12/12 survivable from full budget down to 1/8** (1642 → 62 s, **~0.4 s/replan**), because the reactive-stack θ-basin is wide. The outer loop replans at **0.1 Hz** (`RFLY_REPLAN_DT 10.0`) — so **0.4 s/replan is already real-time by a factor of 25.** The 10 µs bar is inherited from the neural-policy era (a 500 Hz *inner* loop), and nothing about a mission-layer setpoint needs it. **Therefore the ONLY thing standing between GM_RFLY and a deployable flight computer is the PRIVILEGE — which is exactly and solely what ①b measures.** If the blind search holds its rate, a reduced-budget GM_RFLY is shippable today and three arcs of distillation were solving a problem that did not need solving. If it collapses, the privilege *was* the controller, and D-047's constant-θ line becomes the main deployable path — possibly **beating the blind search outright**, which would be the sharpest result available tonight.
- 🟨 **①c D-047 — THE BEST CONSTANT θ: how far does the REFLEX TIER go with no search and no privilege? FLYING.** The question seven weeks of work never asked. `--rfly-fixed` has existed since D-042 but was only ever used as an *ablation* (identity, mean-θ) — **nobody ever optimized the constant.** A constant θ is genuinely deployable: **zero inference, zero privilege, zero latency (0.39 s/run measured vs 76 s for the CEM — 195×), zero new architecture** — it IS the reactive stack with D-030's own knobs re-gained. Hand probes on s42 ×60 first: identity **9/60** (= the known reactive+D-030 baseline, so the rig is validated) · D-046 run-0's converged θ frozen **9/60** (reproduces D-042 exactly) · aggressive divert **5/60** (WORSE) · **divert knobs at the box ceiling 13/60 (BETTER)**. So the landscape has headroom AND is **non-monotone** — partway hurts, all the way helps — which is the shape that rewards a search and defeats the hand-tuning that plateaued D-030. CEM POP=24×ITERS=8 on **training seeds 5000/5001 only** (held-out law: optimizing on 42 and reporting 42 is the easiest fake win in this repo), winner flown on 42/7/99 **once**. Pre-registered: **≤13/60 ⇒ null, the reflex tier is capped · 15-25/60 ⇒ a ten-number zero-privilege controller doubles-to-triples the deployable rate · >25/60 ⇒ "the compound is search-necessary" needs re-scoping.** Honest bound: a constant cannot beat the search (elitism slot 0 *is* the identity warm start, so the population contains every constant) — this measures how much of GM_RFLY's edge came from **adapting** θ versus simply **having a better** θ. Runs single-threaded by design, to cost the 16-core ①b batch ~6% rather than half. `runs/d047_constant_theta_search.py`.
  - **EARLY STRUCTURE (7 evals, 2026-09-11) — why four ADRs of hand-tuning plateaued.** The leader at **37/120** (vs identity ~18/120) is `EKR 4.00 · EKV 3.53 · EBANK 2.31` at/near the **ceiling** while `TLD 0.25 · KDIV 0.40 · KVN 0.50 · KV 0.50` sit on the **floor**. The worst candidates (1–3/120) all carry `KDIV ≈ 2.0`. So the two knob families must move **together, in opposite directions — maximum lateral authority with minimum damping.** That is exactly why my own hand probes missed it: "aggressive divert" (5/60) and "box-ceiling divert" (13/60) both left the damping family at 1.0, where it fights the divert. **A one-family-at-a-time sweep — which is what D-030 was — cannot reach this point**, and the non-monotonicity that defeated hand-tuning is a coupling, not noise.
  - **⚠ BOTH OF MY OWN CLAIMS BELOW ARE CORRECTED BY THE NEXT BULLET — read them together.**
  - **THE QUALITY CAVEAT, recorded before the headline number exists (17 evals, leader 70/120).** Flown on a TRAINING seed (5000, never held-out) the leader lands **35/60 — but 0 PERFECT · 8 GOOD · 27 HARD**, mean td_v **4.09 m/s (max 5.83, against a 6.0 crash threshold)**, lat **11.37 m**, tilt 4.01°. Identity's 9/60 is 1P·3G·5H; the privileged search's 60/60 is 41P·19G at **0.32 m**. **So the objective — raw `LANDED`, which counts HARD — is being optimized by trading quality for quantity and parking on the verdict boundary.** That is a real 4× gain in *survival* and it must not be reported as anything more: these are scrapes, not landings. Two honest consequences: (a) every D-047 number ships with its PERFECT/GOOD/HARD split, never as a bare rate; (b) **D-048 will carry a QUALITY-WEIGHTED objective** (PERFECT/GOOD worth more than HARD) so the follow-on optimizes a landing rather than a survival. Keeping raw `LANDED` here is deliberate — it is the repo's own standard metric in every ADR, and changing it mid-run would be moving the goalposts — but the bare number alone would flatter the result.
  - **CORRECTION (01:16) — MY PREDICTION IS FALSIFIED AND MY CAVEAT WAS TOO HARSH. Both on my own test, on a TRAINING seed.** I registered that the EO-tuned aggression would **regress clean flight**. Flown clean (no fault) on seed 5000 ×60: **identity 49/60 (0P·7G·42 HARD·11 crashed) vs the D-047 leader 59/60 (0P·52 GOOD·7 HARD·1 crashed).** The opposite of the prediction, by ten draws — *and* it converts 42 hard arrivals into 52 good ones. Three consequences, all of them against my earlier claims:
    1. **The "one constant must compromise between divert and flare" story is not supported.** The same θ is better on both batteries. ①e's stated motivation weakens sharply; it now has to justify itself on the EO HARD arrivals alone. Its pre-registered null ("phase is not the axis") just became the likelier branch.
    2. **The quality caveat was wrong as written.** The leader does **not** trade quality for quantity — it *dominates*. On EO it has **8 GOOD vs identity's 4 good-or-better**, and 27 HARD *on top*. It is not degrading landings that were already good; it is converting crashes into (mostly hard) landings. The honest statement is dominance, not a trade. The bare-number caution still stands — always ship the split — but the accusation does not.
    0. **THREE INTEGRITY CORRECTIONS TO MY OWN METHOD (01:20), two of them found by an outside reviewer and confirmed by measurement. Read these before quoting any D-047 number.**
       - **(a) THE WARM START LEAKED A HELD-OUT SEED.** My hand probes (identity 9/60, box-ceiling 13/60) were flown on **seed 42 — which is held-out** — and I then seeded the CEM mean at the box-ceiling θ *because it scored 13/60 there*. That is selection informed by a held-out seed. Mild (I picked a corner of the box, not a tuned point, and the search has since moved far from it — 84/120 vs the warm start's 24/120) but **real, and it makes the held-out number optimistic by an unmeasured amount.** Disclosed rather than quietly re-run; future searches derive their warm start from training seeds only.
       - **(b) WINNER'S CURSE, CONFIRMED AND LARGE.** 84/120 is the max over ~200 noisy samples. Re-scored on **fresh training seeds never optimized against**: **33/60 and 35/60**, against the in-sample **44/60 and 40/60**. A drop of ~8 draws, **−19%**. **The honest expectation for held-out is ~34/60, not 42/60.** Every citation uses the re-scored figure; the in-sample number is a search artifact.
       - **(c) EVALUATION NOISE IS COMPARABLE TO THE SIGNAL BEING SELECTED ON.** At the same seed, EKV 3.859 scored 33/60 while EKV 4.000 scored 41/60 — an 8-draw swing from a 0.14 change, which is ~2σ of binomial noise at n=60 (σ≈3.8; at n=120, σ≈5.4). **So the CEM is partly fitting noise**, and differences under ~8 draws are not distinguishable at this sample size. The 180-draw held-out battery is the only measurement with enough power to be quoted plainly. Future searches use more draws per evaluation, not more iterations.
       - **RETIRED BY MEASUREMENT (the reviewer's other concerns):** *"six of ten knobs sit on the box bounds — is the box binding?"* — **no.** Widening past the box **hurts**: EKR/EKV at 4,4 → 41/60, at 6,6 → 30/60, at 9,9 → 23/60. The bound sits near the true optimum by luck, not truncation. (EBANK is separately clamped at 45° absolute in `control.c`, so it is physically saturated regardless.) And *"θ was tuned on engine-out, check the nominal case"* — **already flown, and it improves**; see the correction bullet above.
    3. **The real finding is bigger than the EO battery: the shipped reactive gains are substantially mistuned, on undisturbed flight too.** **clean ENTRY 49→59/60** (42 HARD → 52 GOOD) and **clean AERO 50→55/60** (0P·13G·37H → **2P·36G·17H**), with no fault anywhere, from nothing but better constants. Every "reactive baseline" number in this repo rests on `identity`, and identity is not a neutral reference — it is a *badly tuned point that was never optimized*. That reframes far more than the engine-out axis, and it touches **M4**: the AERO axis is the one M4 gates, and it was cleared (D-044, 171/180 = 95.0%) by a learned θ̂ net. **If a bare constant moves AERO materially, part of what the net was credited with was simply better gains.** Worth a held-out AERO battery on the final winner — noting that identity's 83.3% here is on a *training* seed and the hoverslam reference (131/180 = 72.8%) is held-out, so the two are not directly comparable and the check has to be run properly rather than inferred.
- ⬜ **①d — θ CONDITIONED ON `n_eng`, and a PRE-REGISTERED PREDICTION about ①c's verify.** D-047's structure (divert family at the ceiling, damping family at the floor) is a compromise forced by using **one** constant for both regimes: low damping is what an engine-out divert wants and is very likely *wrong* for undisturbed flight. **Prediction, registered before `d047_verify.py` runs: the ①c winner will REGRESS on clean ENTRY and clean AERO against identity, even as it gains on the EO battery.** If it does, the diagnosis is not "constant θ is a dead end" — it is "one constant cannot serve two regimes," and the fix is the cheapest possible conditioning: **two θ vectors selected on `n_eng<3`** (20 numbers, not 10). That switch is legitimate by construction — `n_eng`/`eng_health` are *directly observed* (OBS_EH0/EH1/EH2), it needs no inference and no privilege, and **D-030 already proved this is the right switch.** Still zero-latency, still a header of numbers, still no net. If instead the winner holds clean flight, the compromise was cheaper than expected and ①d drops in priority.
- ⬜ **①e — PHASE-SCHEDULED θ (`--rfly-fixed-phase`, 3 bands × 10). PROMOTED ABOVE ①d, on a measurement.** The fault fires at t ∈ [4,18] s of a **~117-140 s** flight, so **`n_eng<3` holds for ~90% of every draw on this battery** — ①d's switch barely discriminates *here* (it still protects clean flight and stays available). The axis the ①c quality data actually points at is **phase**: the entry divert wants maximum lateral authority and minimum damping; the terminal flare wants exactly the opposite; one constant must pick, picks aggressive because that is what converts an off-pad crash into a landing at all, and pays for it at touchdown with 0 PERFECT and 4.09 m/s arrivals. **D-042 said the same thing from the other side** — run-0's own converged θ held constant crashes its own draw. Phase is `State.phase`, the vehicle's own flight state: no inference, no privilege, no net, thirty numbers. Objective is **quality-weighted** (P3/G2/H1) so it cannot repeat ①c's boundary-parking. Warm start = all three bands at ①c's winner, so plain ①c is exactly representable and sits in elitism slot 0 ⇒ **can only match-or-beat**. `runs/d049_phase_theta.py`; C written (`sim.c` arming site), builds when the batches release the exe. **Pre-registered: better weighted at similar rate ⇒ the flare band recovers the precision the single constant traded away (predicted) · no better ⇒ phase is not the axis and the HARD arrivals are the price of landing at all, ①e closes.** D-042 proved θ must vary by phase (run-0's own converged θ held constant crashes its own draw). A single constant therefore has a structural ceiling. The next rung that is *still* zero-inference and zero-privilege: **a small lookup table — θ per flight phase** (entry burn / aero descent / landing burn), 3×10 numbers instead of 10. Same search, same held-out discipline. This is the cheapest thing between "one constant" and "a learned schedule", and it needs no net at all.
- ⬜ **② THE REACHABILITY MARGIN — a tap, NOT a net.** The CEM already computes survivable-fraction ~4,400×/flight and discards it (`guidance_rfly.c:107` fills `cost[]` in candidate order under `#pragma omp parallel for`, sorts, keeps the argmin). Tap it **after** the OMP loop — race-free, no locking — and emit **fraction-reaching-`S_land`** as a live scalar in [0,1]. Ground truth = `ceiling_eo.c`. Needs its OWN tap handle: `rfly_eval_candidate` nulls `c2.tap.f` (`:65`) and the async launch nulls the whole snapshot's (`:185`), so a value tap riding `Sim.tap` is silently dead on the async path — the exact shape of the D-041 `c2.guidance_mode` bug. Default-off, byte-clean, leak-GREEN. **BUILD IT BLIND FROM THE START (D-046):** a survivable-fraction computed from *clairvoyant* rollouts launders foreknowledge into a scalar and hands it to a controller that cannot legitimately have it — the same wall Phase 3 hit, one layer down. Gate ①b first.
- ⬜ **③ THE SCRIPTED MISSION LAYER + THE COMMITMENT HORIZON.** Deterministic rules on ②'s margin: below M, re-select from the reachable alternates; no alternate ⇒ soft-ground abort. **This is where the number probably is** — D-027's phase attribution puts ~75% of the gross cluster at the ENTRY-BURN CUT (2-engine divert closes only ~830 m): the vehicle commits to a pad it can no longer reach and never re-decides. Also replaces the hard TERMINAL FREEZE (`guidance_rfly.c:166`, a phase gate that discards every legitimate late revision) with a **revision-latency contract** — freeze when margin ≈ 0, revise above it; one hard-coded gate becomes a measured continuous quantity, and it is the same number the abort reads. **If the script gets it, the writ's "scripted arm" was the answer, not the null.**
- ⬜ **④ THE LEARNED PAIR — only after ③ has a score to beat.** (a) **generative θ̂**: emit K samples seeding the CEM's *population*, not its mean — every learned artifact this repo built is a deterministic regressor onto a multimodal target (the trainer's own note: *the conditional mean is mush*; LODESTAR's mode-averaging wall), and R2b went NEGATIVE as budget shrank (12→10→9) for exactly this reason. (b) **the ranking head** over `(obs, θ_candidate) → survivable-fraction` — single-valued because the evaluator is pure in its arguments, and already in [0,1] so no monotone transform is needed. **Mandatory uniform random arm + a logged column for how often the winner came from it** — otherwise the lottery carries the result and the aggregate hides it. Gate on rank correlation + landing rate at **matched wall-clock**, never on val error (D-044: TP_VERSION 2 has WORSE val_nrmse than v1 and lands +9).
- ⬜ **⑤ VISION — last, and scoped to organ ③ ONLY.** The control loop is 500 Hz / guidance 50 Hz / MPPI 10 Hz; a native early-fusion glance closes at 6-10 Hz on a free card and 0.6-24.6 s on a loaded one ⇒ **architecturally excluded from ①②④**, and the failure isn't perceptual anyway (D-027: in-frontier). But **choosing ground is the one genuine perception problem in this vehicle**, it runs at ~1 Hz, and nothing aboard has ever looked at the world and picked a site. Probe-scalars on the residual (~44 ms, no decode, no caption), G-buffer for geometry, the eye for judgment, **the reachability oracle as the veto** — a hallucinating VLM can only nominate ground that `ceiling_eo.c` then refuses. `runs/perception_design.md` specs most of this (written 2026-07-19, never built). **Showcase axis, not the frontier axis — orthogonal to the ~59/60 gap.**

## PLANT HONESTY — open, unrepaired (operator-reported 2026-09-08, verified; full receipts in D-046)
- ⬜ **The moving target is FED, not sensed.** `sim.c:411/:433/:445` write the deck's exact truth position into `gcmd.target_xy` with `target_valid=1, target_age=0.0`; `nav.c:78` confirms `--nav-noisy` adds **no** target noise. **`target_age` is a structural zero** — assigned `0.0` at three sites and nowhere else, yet it holds protocol offset 264 and feeds the policy as `OBS_TAGE`: **one of the 39 observation channels is a constant in every net trained this arc.** The hardest axis in N3 is graded against a perfect instantaneous oracle.
- ⬜ **No actuator or sensor lag, either direction.** `control.c:186-195` algebraically inverts torque into gimbal angles and applies them the **same tick** — no slew rate, no deflection dynamics; likewise fins/RCS. `nav.c` has noise + gyro-bias random walk (`:115-126`) but **no transport delay**.
- **Consequence, so it is not overclaimed later:** `ceiling_eo.c` computes D_phys against *this* plant, so **a BRS with actuator lag is strictly smaller** and D-027's "in-frontier ≈ 1.000" is an upper bound resting on an optimistic plant. It does not rescue a ~50-draw gap, but part of that gap is a gift, and the frontier shrinks when either is repaired.

## PARALLEL-SAFE (any farm-free or frontend window)
- ✅ **Target Stage-1 COMPLETE (D-034→D-037)** — the moving-target axis N3 needs is DELIVERED: a deterministic, byte-clean, replayable moving deck (heave + drift + deck-relative leg loads + target-relative verdict) streamed to the renderer over the unchanged v4 protocol.
  - ✅ **Renderer (FE-SEA, `d6f37cb`):** the three.js documentary scene now VISUALIZES it — a Gerstner-wave ocean + the ASDS droneship deck posed EXACTLY from `deck_z`/`target_est_xy` (heaves + drifts as the plant computes), toggled by the SEA-active flag. Gates green (typecheck, 170 tests incl. 7 Gerstner, build); runtime-verified via eval (deck pose + ocean displacement). **Pixels need eyes-on** (`runs/fe_sea_report.md` §tuning: palette/foam/proportions/deck-tilt). Deck TILT awaits the sim streaming `deck_quat` (§F).
  - ✅ **Stage-1a — §A.3 target-relative verdict (D-034):** verdict + touchdown `impact_lat` now measure from the target pose LATCHED at first contact (`impact_target_xy=gcmd.target_xy`), not the origin. FIXED target = (0,0) ⇒ byte-identical (leak GREEN). Proof: `--target line:30:10:0 --mppi` lands `td_lat 21.58 m` target-relative (on-pad; was ~30 m origin off-pad); honest moving-target rate `--target circle:15:60 --neural` ×60 = 6/60 (policy is clean-trained on a FIXED origin ⇒ weak tracking = N3 curriculum, not a verdict bug). No NP_VERSION bump.
  - ✅ **Stage-1b — SEA deck (D-035):** `core/sea.{h,c}` P-M spectrum (48 comp, equal-energy, `RNG_SEA`) + `deck_z(t)`/`deck_vz(t)` deck-frame leg loads (§A.2) + **§A.4 Option-i deck-aware guidance** (`h_base=y_z−deck_z−com`). Byte-clean (leak GREEN both builds; calm-floor 58/60, det-pair identical). Heaving-deck landing rate lifts ~9–13 pp deck-blind→deck-aware across seeds/Hs: hoverslam Hs3 95→**114**/180, Hs1.5 113→**129**, neural Hs1.5 93→**116**. Nominal run 0 flipped CRASHED(fuel)→GOOD. Remaining: ±3 m WANDER + `target_xy`→guidance (Stage-1c), tilted-normal contact (§F polish), MPPI deck-aware rollout, protocol marker (§C). No NP bump.
  - ✅ **Stage-1c — SEA horizontal station wander (D-036):** the deck's slow ±wander (seeded, ~40–80 s periods) drives `gcmd.target_xy` (like MOD_TARGET), so the deck HEAVES + DRIFTS and the vehicle tracks all axes. `--sea-wander [amp]`. Byte-clean (wander off ⇒ heave-only PROVEN identical: heave-only aggregate exactly reproduces the D-035 batch 43/33/38/43/37). The ±3 m wander costs only **−2.2 pp** (hoverslam Hs1.5 heave-only 129 → heave+wander **125/180**, 69.4%) — slow station-keeping is nearly free (Option-i thesis, horizontal). Det-pair 42==42. **⇒ the deterministic moving deck is DONE (heave+drift+deck-relative legs+target verdict, all byte-clean).**
  - ✅ **Stage-1d — renderer marker (D-037): NO protocol bump needed.** The v4 WIDE SOCKET already carries `target_est_xy@232` + `deck_z@304`; `fill_tlm` populates them from the nav socket the SEA block writes. VERIFIED by live WebSocket TLM capture (`--sea 1.5 --sea-wander 3`): `target_est_xy=(−2.9,2.9)` within ±3 m wander, `deck_z=−0.19` live-changing, `src=TGT_SEEDED` (`runs/d037_wire_capture.txt`). The moving deck reaches the wire.
  - ⬜ **Stage-1 polish (not the axis):** tilted-normal contact from deck pitch/roll (§F); MPPI rollout-level deck-awareness; heave-phase-timed terminal commit (the D-035 hover-hunt caveat). **fast-target `target_vxy` lead = D-038 NULL** (the §F.6 one-liner `−Kvd(v_xy−target_vxy)` REGRESSES: slow circle 55 vs 56, fast circle **0 vs 12** of 60 — the seek+damp law needs the lead on the `vdes` SEEK not the damping, + re-tune; reverted). Slow SEA wander already tracks near-free without it (D-036), so the deployed law is fine for the canonical deck.
- ✅ **Interactive ws command channel (Mode 2, `1db65ac`+`766c607`+`8ebb090`)** — the live failure-injection demo instrument. Fenced behind `--interactive` (default OFF = pure-observer, byte-identical, goldens GREEN); ON = inbound `BlCmd` frame (protocol.h 'CMD0') parsed in `ws_poll_client`, applied to the LIVE sim (`sim_set_gust` at current alt / `arm_engine_out` at current t), each injection journaled `[INJECT] t=..`. UI: `hud/injectPanel.ts` **3 buttons — WIND GUST / ENGINE OUT / THRUST LOSS** (`BL_CMD_*` 1/2/3) → `client.send(BlCmd)`. Verified end-to-end (browser button → core `[INJECT]`; all three). The shell spawns `--interactive` so the portable app's buttons work. **Target-drag SHIPPED (D-039): `BL_CMD_TARGET`** — the operator drags the pad, guidance chases it (live WS capture: injected (40,25) → `target_est_xy` flips `(0,0) FIXED → (40,25) SEEDED`, `runs/d039_drag.txt`); byte-clean (serve-only). Remaining: frontend drag→BlCmd wiring + replay-from-journal (§10.8), then UE 5.8 on the same wires.
- ✅ **Renderer bloom (`686cfa0`)** — the flagged §5.2 glow, now wired: `scene/postfx.ts` `PostProcessing` + `bloom(pass(scene,camera))` (three r185 TSL), so the HDR plume / green-flash / hot markers bloom (AgX+sRGB at output). Tunable `BLOOM` + DEV `__postfx.set()`; runtime-verified (renders clean); **intensity needs eyes-on against a live burn**.
- ✅ **FE photoreal-v2 (2026-07-23): the "eyes-on" pass finally ran — by the agent's own eyes.** The old "pixels need eyes-on" caveat is retired: the browser-pane loop (`?raf` headless-drive + `__shotPoseHDR` + `/__cap` + an in-page condition watcher) drove ~12 iterate-verify rounds solo. Shipped: TSL node-material OCEAN (procedural animated normal octaves + laced crest foam + real deep-water albedo, no more vertex-color blobs) · TSL procedural SOOT hull (streaky, bottom-heavy; bound ONCE with a tankLen uniform) · real A-frame LEGS + lathe Merlin bells + open interstage + finer Ti grid fins · ASDS deck plating/rust/yellow-perimeter/white bullseye · plume white-hot core + diamond pinch + fixed balloon curve (was half-ballooned at sea level) · SHADOWS enabled + sun-follow (the `updateProjectionMatrix` trap) · photographic exposure re-balance (sun 1.9/exposure 0.34 — the "white vehicle" was blown sunlit paint, probe-verified). Plus the self-verify harness hardening: WebGPU async-pipeline white-placeholder mitigations (RT-bound compileAsync, RT cache, double-render settle), `__shotProbe` raw-pixel ground truth, `__markers(on)` beauty switch. Gates: typecheck, vitest 174/174, build. Inject buttons re-verified end-to-end (`[INJECT] GUST` journaled).
- ✅ **Renderer ocean + ASDS droneship deck (FE-SEA, `d6f37cb`)** — see the Target Stage-1 line above; the moving deck is now VISUALIZED.
- ✅ **Portable app (`8ebb090`, `runs/portable_app_report.md`)** — `booster-shell.exe` (Tauri, UI embedded) + core sidecar = a copy-anywhere `dist/BoosterLander-portable/` (~11.7 MB); double-click spawns core `--serve --interactive` in one shot, kill-on-close (no orphans). Built via `cargo build --release`.
- ✅ **`--neural --nav-noisy` honesty spot + np_version plumbing (D-033):** neural nav-noisy AERO 46→44, ENTRY 57→55 (graceful −2, robust — not a truth-state artifact); np_version now carried in HELLO/TLM under GM_NEURAL (byte-clean; goldens emit under hoverslam so unchanged).

## HARD LAWS (violating = quality loss; full text: handoff §4 · CLAUDE_v2 · `C:\Users\user\.claude\WINDOWS_SHELL_GUIDE.md`)
- **Held-out law:** s42/s7/s99 NEVER in training data (trainer enforces). **KAT from the C pass, never numpy.** Every export = NP_VERSION bump = ADR + KAT + re-golden.
- **Windows/pwsh:** detached = `Start-Process` (never `Start-Job`); watch an artifact/marker (never a bare PID — Windows recycles them); `pwsh` never `powershell`; check `$LASTEXITCODE`; never build over a live farm (LNK1104 stale-exe); `--serve` needs explicit `--port 8787`.
- **Gates:** selftest · TERMINAL ×200 byte vs `runs\n0main_terminal.txt` · MPPI run-1 AERO s42 = HARD 2.63/10.48 · determinism pairs · leak byte-equality. Dirty byte-gate ⇒ investigate, NEVER commit. **The MPPI anchor is `--run --scenario aero_offset --seed 42 --run 1 --mppi` and greps `RESULT` — `--run` is a MODE (`main.c:1291`), so the `--headless … --run 1` form silently runs NOTHING and exits 0.**
- **FARM-SCRIPT LAW (D-046 add.1 — three false greens in one night, all the same class: a command that did nothing, reporting success).** Every unattended batch script MUST: **(1) verify** — each unit yields its expected result line or it is marked FAILED and the run stops; a DONE marker is written ONLY when every unit verified (the 09-09 run wrote DONE on zero data and the monitor believed it); **(2) hold the box awake** — `SetThreadExecutionState(ES_CONTINUOUS|ES_SYSTEM_REQUIRED)`, because the kernel power manager killed a 3.8 h farm at 04:44 and the box stayed down a day; **(3) be resumable** — per-unit result files, so a kill resumes instead of restarting; **(4) keep stderr** — per-unit `.err` files, because a silent death into a hidden window is undiagnosable. **And a monitor must check the DATA, never the marker.** *"Found nothing" and "broke" must never be indistinguishable* — the estate's own law, never before applied to its own farm scripts, which is exactly where overnight work lives. Also beware PowerShell parsing `0x80000000` as signed Int32 (`-bor` ⇒ −2147483647, P/Invoke refuses it) — type uint32 constants explicitly.
- **FARM-SCRIPT LAW, CLAUSE 5 — A MONITOR'S LIVENESS CHECK MUST NAME ITS OWN JOB (found 2026-09-11, the fourth instance of the same class).** The D-047 monitor's death branch was `if ! tasklist | grep -qi python`. D-050 was *also* a python process, so the check matched something else and **never fired**: d047 died at 02:14 and the monitor polled for **seven hours** reporting "No output yet", which the operator correctly read as *finished, stalled, or never started — indistinguishable.* **A liveness check that can be satisfied by a process other than the one being watched cannot detect death.** Match the command line, not the image name. This is the same defect as the other three — **a check too broad to ever fail** — and it is the one that survived writing the law, because the law was aimed at the *script* and this was in the *watcher*.

*Ledger arc so far: D-019 canon → N0/N1 pipeline → D-023 student beats teacher clean → D-024 beats under shear → D-025 teacher collapses on EO → D-027 collapse is recoverable ground → D-028 ENTRY solved at parity. One clean arc (E1→N3) from the showcase.*
